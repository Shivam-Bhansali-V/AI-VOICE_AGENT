SHEET_NAME=PeekCAT Call Logs
PORT=8000
# For Railway deployment, add this variable with your actual credentials JSON:
# CREDENTIALS_JSON={"type":"service_account",...full json...}
# For local development, ensure credentials.json file exists in the project root
